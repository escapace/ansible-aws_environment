---

galaxy_info:
  role_name: aws_environment
  description: aws_environment role
  issue_tracker_url: https://github.com/escapace/ansible-aws_environment/issues
  license: Apache-2.0
  author: escapace
  min_ansible_version: "2.13"
  platforms:
    - name: EL
      versions:
        - "9"

  galaxy_tags:
    - escapace
    - aws

dependencies:
  - { role: escapace.aws_cli }
  - { role: escapace.escapace_releases }
